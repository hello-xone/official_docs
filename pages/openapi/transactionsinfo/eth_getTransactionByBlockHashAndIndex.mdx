---
description: >-
  Retrieves a specific transaction from a block using the block's hash and the
  transaction's index position
keywords: eth_getTransactionByBlockHashAndIndex
---
# eth_getTransactionByBlockHashAndIndex

Retrieves a specific transaction from a block using the block's hash and the transaction's index position

import { EthereumMethod_getTransactionByBlockHashAndIndex } from "components/method-docs/ethereum/EthereumMethod_getTransactionByBlockHashAndIndex";

<EthereumMethod_getTransactionByBlockHashAndIndex />

# eth_newPendingTransactionFilter

创建一个过滤器，用于在新待处理交易添加到交易池时发出通知。

import { EthereumMethod_newPendingTransactionFilter } from "components/method-docs/ethereum/EthereumMethod_newPendingTransactionFilter";

<EthereumMethod_newPendingTransactionFilter />

import { Steps } from 'nextra/components';
import { Link, Code } from "@nextui-org/react";
import { Callout } from 'nextra/components'
import ImageViewer from '@components/ImageViewer';
import { Tabs } from 'nextra/components'

# 部署 ERC-1155 NFT 合集

本指南将引导您完成使用 Remix IDE、OpenZeppelin 和 Pinata.cloud 存储 NFT 元数据来部署名为 **"Astronaut"** 的 ERC-1155 NFT 合集的过程。

**"Astronaut"** 合集将具有以下特点：

-   无总供应量限制。
-   初始供应量为 0。
-   要求用户自行铸造 NFT。

## 先决条件

1.  配置好 Xone Chain 网络的 <Link className='text-primaryHue' isExternal showAnchorIcon href="链接8">MetaMask 钱包</Link>。
2.  打开 <Link className='text-primaryHue' isExternal showAnchorIcon href="链接3">Remix IDE</Link>。
3.   用于支付 Gas 费的测试网 XOC（Xone Chain 测试代币）。使用 <Link className='text-primaryHue' isExternal showAnchorIcon href="链接4">Xone 水龙头</Link> 获取免费代币。
4.   获取 <Link className='text-primaryHue' isExternal showAnchorIcon href="链接5">Pinata</Link> 账户，如果您有自己的存储服务器，可以跳过此步骤。
5.   下载 <Link className='text-primaryHue' isExternal showAnchorIcon href="https://github.com/hello-xone/official_docs/raw/refs/heads/main/public/zip/Astronaut.zip">Astronaut</Link> NFT 压缩文件。

<ImageViewer src="/images/Create ERC-1155.png" alt="Create ERC-1155" />

## 开始吧
<Steps>

### 步骤 1：创建 Astronaut NFT 合约

1.  打开 Remix IDE，在 `contracts` 文件夹下创建一个新文件。将其命名为 `Astronaut.sol`。
2.  将以下代码添加到文件中：

``` js copy showLineNumbers filename="Astronaut.sol"
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;
 
import "@openzeppelin/contracts/token/ERC1155/ERC1155.sol";
import "@openzeppelin/contracts/access/Ownable.sol";
import "@openzeppelin/contracts/utils/Counters.sol";
 
contract Icecream is ERC1155, Ownable {
    using Counters for Counters.Counter;
    Counters.Counter private _tokenIds;
 
    string public name = "Astronaut";
    string public symbol = "AST";
    string private constant _uri = "https://"; // Replace with your metadata URI
 
    constructor() ERC1155(_uri) Ownable(msg.sender) {}
 
    function mint(uint256 amount) public {
        _tokenIds.increment();
        uint256 newItemId = _tokenIds.current();
        _mint(msg.sender, newItemId, amount, "");
    }
 
    function mintBatch(uint256 numberOfTokens) public {
        uint256[] memory ids = new uint256[](numberOfTokens);
        uint256[] memory amounts = new uint256[](numberOfTokens);
        
        for (uint256 i = 0; i < numberOfTokens; i++) {
            _tokenIds.increment();
            ids[i] = _tokenIds.current();
            amounts[i] = 1;
        }
        
        _mintBatch(msg.sender, ids, amounts, "");
    }
 
    function uri(uint256) override public pure returns (string memory) {
        return _uri;
    }
}
```

<ImageViewer src="/images/Create ERC-1155_0.png" alt="Create ERC-1155_0" />

### 步骤 2：使用 Pinata 存储 NFT 元数据

1.  注册并登录 <Link className='text-primaryHue' isExternal showAnchorIcon href="链接5">Pinata</Link>。如果您还没有账户，请提前注册。
2.  将 Astronaut 图片上传到 Pinata。没有 Astronaut NFT 图片？<Link className='text-primaryHue' isExternal showAnchorIcon href="https://github.com/hello-xone/official_docs/raw/refs/heads/main/public/zip/Astronaut.zip">点击此处</Link> 下载 Astronaut NFT 图片源文件。
3.  在 Pinata 仪表板中，点击【**Upload**】，然后选择【**Folder**】。
4.  上传 **Astronaut** 源文件夹。

<ImageViewer src="/images/Create ERC-1155_1.png" alt="Create ERC-1155_1" />

<Callout emoji="💡">
  记下此 CID URL <Code color="default">链接6</Code>，铸造 NFT 时会用到它。
</Callout>

5.  创建元数据 JSON 文件

    创建一个具有以下结构的 JSON 文件。不确定如何创建？下载 <Link className='text-primaryHue' isExternal showAnchorIcon href="https://github.com/hello-xone/official_docs/raw/refs/heads/main/public/zip/Astronaut_NFT_metadata.zip">Astronaut NFT 元数据</Link> 源文件。

``` js showLineNumbers copy filename="example.js"
{
  "name": "astronaut",
  "description": "Martian Astronaut NFT",
  "image": "https://orange-cheap-pony-550.mypinata.cloud/ipfs/QmVwRzoHkYWeVHu7Jff9KZ18FCvBW4uSvUHokFa1T4BuWf/astronaut.png"
}
```
6.  将元数据 JSON 上传到 Pinata.cloud

<ImageViewer src="/images/Create ERC-1155_2.png" alt="Create ERC-1155_2" />

7.  获取 `CID_of_your_metadata`

`链接1`

### 步骤 3：更新 Astronaut NFT 合约

替换这一行：`string private constant _uri = "https://";`

``` js {14} copy showLineNumbers filename="Astronaut.sol"
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;
 
import "@openzeppelin/contracts/token/ERC1155/ERC1155.sol";
import "@openzeppelin/contracts/access/Ownable.sol";
import "@openzeppelin/contracts/utils/Counters.sol";
 
contract Icecream is ERC1155, Ownable {
    using Counters for Counters.Counter;
    Counters.Counter private _tokenIds;
 
    string public name = "Astronaut";
    string public symbol = "AST";
    string private constant _uri = "https://orange-cheap-pony-550.mypinata.cloud/ipfs/QmQgdeWbjostba4Y8bAK4bgxvXfLHpfTmAGEcHUvnf9K99/astronaut.json";
 
    constructor() ERC1155(_uri) Ownable(msg.sender) {}
 
    function mint(uint256 amount) public {
        _tokenIds.increment();
        uint256 newItemId = _tokenIds.current();
        _mint(msg.sender, newItemId, amount, "");
    }
 
    function mintBatch(uint256 numberOfTokens) public {
        uint256[] memory ids = new uint256[](numberOfTokens);
        uint256[] memory amounts = new uint256[](numberOfTokens);
        
        for (uint256 i = 0; i < numberOfTokens; i++) {
            _tokenIds.increment();
            ids[i] = _tokenIds.current();
            amounts[i] = 1;
        }
        
        _mintBatch(msg.sender, ids, amounts, "");
    }
 
    function uri(uint256) override public pure returns (string memory) {
        return _uri;
    }
}
```

<Callout type="warning" emoji="⚠️">
  此步骤通过为合集中的所有代币定义统一的元数据 URI，来确保 ERC-1155 NFT 合集的一致性。

  通过设置通用 URI，从该合约铸造的所有 NFT 将共享相同的元数据结构，包括名称、描述和图像等关键细节。这种标准化简化了 NFT 元数据的管理和检索，为用户和收藏者提供一致且无缝的体验。
</Callout>

### 步骤 4：编译智能合约

1.  点击左侧边栏的【**Solidity Compiler**】选项卡。
2.  选择适当的编译器版本（0.8.x）。例如：`0.8.26+commit.8a97fa7a`。
3.  点击【**Advanced Configurations**】以展开高级设置。
4.  选择 EVM 版本为【**Paris**】。
5.  启用优化（默认 200）。
6.  点击【**Compile FruitNFT.sol**】按钮。确保没有编译错误。

<ImageViewer src="/images/Create ERC-1155_3.png" alt="Create ERC-1155_3" />

### 步骤 5：部署智能合约

1.  在 Remix 中导航到【**Deploy & Run Transactions**】选项卡。
2.  在 **ENVIRONMENT** 下拉菜单中，选择 **Injected Provider - MetaMask** 以将 Remix 连接到您的 MetaMask 钱包。
3.  从下拉菜单中选择 FruitNFT 合约。
4.  点击【**Deploy**】并在您的 MetaMask 钱包中确认交易。

<ImageViewer src="/images/Create ERC-1155_4.png" alt="Create ERC-1155_4" />

### 步骤 6：查看合约地址

确认后，您将在 Remix 控制台中看到已部署的合约地址。让我们看一下我们构建的演示：<Link className='text-primaryHue' isExternal showAnchorIcon href="链接2">AstronautNFT</Link>。

<ImageViewer src="/images/Create ERC-1155_5.png" alt="Create ERC-1155_5" />

### 步骤 7：铸造您的 NFT

<Tabs items={['铸造单个代币', '批量铸造代币']}>
  <Tabs.Tab>
  1. 部署后，在已部署合约部分找到 `mint` 函数。
  2. 输入您希望铸造的 NFT 数量（例如，1）。
  3. 点击【**Transact**】并在 MetaMask 中确认交易。
  4. 交易完成后，访问区块链浏览器验证铸造是否成功。跟随我们一起查看一个<Link className='text-primaryHue' isExternal showAnchorIcon href="https://testnet.xonescan.com/tx/0xd84d728acfbf1e6fd8eb75c236f31d20373fd1aafb65116c38c6690719e89089">成功铸造的 Astronaut NFT</Link>。
  <ImageViewer src="/images/Create ERC-1155_7.png" alt="Create ERC-1155_7" />
  </Tabs.Tab>
  <Tabs.Tab>
  1. 在已部署合约部分找到 `mintBatch` 函数。
  2. 输入您想要铸造的新代币 ID 数量（例如，5）。
  3. 点击【**Transact**】并在 MetaMask 中确认交易。
  4. `mintBatch` 函数支持在单笔交易中铸造多个 NFT，从而节省 Gas 费。
  <ImageViewer src="/images/Create ERC-1155_8.png" alt="Create ERC-1155_8" />
  </Tabs.Tab>
</Tabs>

### 步骤 8：将 NFT 添加到 MetaMask

1.  打开 MetaMask 并点击【**Import NFT**】。
2.  粘贴您部署的代币的 NFT 合约地址（在此例中为 `0x95B38c21Cba6D9F8b2dDc87479631cb2CBB6c3D8`）。
3.  输入创建的 NFT 代币 ID（在此例中为 `2`）。
4.  确认后，点击【**Import**】。
5.  如果您的地址拥有此 NFT，您将在 **NFTs** 列表中看到它。

<ImageViewer src="/images/Create ERC-1155_9.png" alt="Create ERC-1155_9" />

### 步骤 9：恭喜！您已成功掌握 ERC-1155 合约的创建。

Astronaut 系列演示，<Link className='text-primaryHue' isExternal showAnchorIcon href="链接2">去看看</Link>。

<ImageViewer src="/images/Create ERC-1155_10.png" alt="Create ERC-1155_10" />

</Steps>

## 演示资源

-   <Link className='text-primaryHue' isExternal showAnchorIcon href="链接2">Astronaut 智能合约</Link>
-   <Link className='text-primaryHue' isExternal showAnchorIcon href="https://github.com/hello-xone/official_docs/raw/refs/heads/main/public/zip/Astronaut.zip">Astronaut 图片</Link>
-   <Link className='text-primaryHue' isExternal showAnchorIcon href="https://github.com/hello-xone/official_docs/raw/refs/heads/main/public/zip/Astronaut_NFT_metadata.zip">Astronaut 元数据</Link>
-   Pinta URL: <Link className='text-primaryHue' isExternal showAnchorIcon href="链接6">Astronaut 图片</Link>、<Link className='text-primaryHue' isExternal showAnchorIcon href="链接7">元数据</Link>